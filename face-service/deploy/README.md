# Deploy Face Service

Gunakan dokumen utama ini:

- `docs/panduan-deploy-semua-fitur.md`

Di dokumen itu sudah ada versi `aaPanel-only` untuk:

1. persiapan folder `/www/wwwroot/face-siaps`
2. pembuatan virtualenv
3. download model
4. isi `Supervisor` untuk `siaps-face-service`
5. contoh output sukses `curl /health`
